# Positioning memory — example

Filled-in example of the GTM planner output (`update-positioning` payload) for a
fictional operator, "Auditlens." Use this to sanity-check that a completed
positioning memory looks like this before wiring the real form.

## One-liner

Auditlens turns every LLM call your app makes into a graded trace, so you ship
prompt changes with proof instead of vibes.

## Proof points

- **technical:** Auditlens ingests traces via a single OpenTelemetry-compatible SDK call and auto-scores 100% of production LLM calls against your eval rubric in under 200ms of added latency.
- **roi:** Teams on Auditlens cut prompt-regression incidents by 63% and dropped manual QA review time from 6 hours/week to 45 minutes/week per engineer.
- **gtm:** Auditlens runs in production behind 40+ AI features at companies shipping weekly, with customers' first-pass eval rate rising from 71% to 94% within the first month.

## Tone rules

- Plain speech, no superlatives ("best-in-class", "revolutionary", "game-changing")
- Sentence case in headlines; no ALL CAPS except acronyms (API, SDK, QA)
- Lead with the number, not the adjective
- Speak to the reader's role directly ("your on-call engineer," not "stakeholders")
- Never say "AI-powered" as if that's the differentiator — the eval rigor is

## Forbidden claims

- Do not claim Auditlens trains, fine-tunes, or hosts any LLM models
- Do not claim Auditlens can auto-fix or auto-rewrite prompts without human review
- Do not claim 100% accuracy or "zero hallucinations" detection
- Do not claim SOC 2, HIPAA, or other compliance certifications unless explicitly confirmed
- Do not claim compatibility with "every framework" — name only shipped integrations (LangChain, OpenAI SDK, raw HTTP)

## Target verticals

devtools, fintech, healthtech, security, data/infra, martech
